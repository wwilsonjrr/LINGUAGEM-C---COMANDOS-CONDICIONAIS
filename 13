/*  13. Faça um algoritmo que calcule a média ponderada das notas de 3 provas. A primeira e
 a segunda prova têm peso 1 e a terceira tem peso 2. Ao final, mostrar a média do aluno
 e indicar se o aluno foi aprovado ou reprovado. A nota para aprovação deve ser igual ou
 superior a 60 pontos.  */


#include <stdio.h>
#include <stdlib.h>

int main()
{
    float media_ponderada, nota1, nota2, nota3;
    int peso1 = 1, peso2 = 2;

    printf("Digite a nota da prova 1 do aluno: \n");
    scanf("%f", &nota1);

    printf("Digite a nota da prova 2 do aluno: \n");
    scanf("%f", &nota2);

    printf("Digite a nota da prova 3 do aluno: \n");
    scanf("%f", &nota3);

    media_ponderada = (nota1*peso1 + nota2*peso1 + nota3*peso2)/(peso1 + peso1 + peso2);

    if (media_ponderada >= 60.0 && media_ponderada <= 100.0)
    {
        printf("A media ponderada do aluno eh %.2f e ele foi aprovado. \n", media_ponderada);
    }
    else if (media_ponderada >= 0.0 && media_ponderada < 60.0)
    {
        printf("A media ponderada do aluno eh %.2f e ele foi reprovado. \n", media_ponderada);
    }
    else
    {
        printf("Valor invalido! \n");
    }

    system("pause");
    return 0;
}
