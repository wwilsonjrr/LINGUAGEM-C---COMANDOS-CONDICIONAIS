/*  25. Calcule as raízes da equacão de 2o grau.
 Lembrando que:

 x = (-b +/- sqrt(delta))/2*a

 onde:

 delta = b*b - 4*a*c

 E a*x*x + b*x + c = 0 representa uma equação de 2o grau.

 A variável a tem que ser diferente de zero. Caso seja igual, imprima a mensagem “N˜ão
 é equação do 2º grau.

 --> Se a < 0, n˜ão existe real. Imprima a mensagem Não existe raiz.
 --> Se a = 0, existe uma raiz real. Imprima a raiz e a mensagem Raiz unica.
 --> Se a > 0, imprima as duas raízes reais.

 ||*/


#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    float a, b, c, delta, x1, x2;

    printf("=== Calculo das raizes da equacao do 2o grau ===\n");
    printf("Digite os valores de a, b e c: ");
    scanf("%f %f %f", &a, &b, &c);

    // Verifica se é do segundo grau
    if (a == 0)
    {
        printf("Nao eh equacao do segundo grau.\n");
    }
    else
    {
        delta = pow(b,2) - 4*a*c;

        if (delta < 0)
        {
            printf("Nao existem raizes reais.\n");
        }
        else if (delta == 0)
        {
            x1 = (-b - sqrt(delta))/ (2*a);
            printf("Raiz unica: %.2f\n", x1);
        }
        else
        {
            x1 = (-b - sqrt(delta)) / (2*a);
            x2 = (-b + sqrt(delta)) / (2*a);
            printf("Duas raizes reais: x1 = %.2f e x2 = %.2f\n", x1, x2);
        }
    }

    system("pause");
    return 0;
}

