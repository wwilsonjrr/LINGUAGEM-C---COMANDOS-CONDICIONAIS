/*  8. Faça um programa que leia 2 notas de um aluno, verifique se as notas s˜ão válidas e
 exiba na tela a média destas notas. Uma nota válida deve ser, obrigatoriamente, um
 valor entre 0.0 e 10.0, onde caso a nota n˜ão possua um valor válido, este fato deve ser
 informado ao usuário e o programa termina.  */


#include <stdio.h>
#include <stdlib.h>

int main()
{
    float nota1, nota2, media;

    printf("Digite as duas notas do aluno: \n");
    scanf("%f %f", &nota1, &nota2);

    if ((nota1 >= 0.0 && nota1 <= 10.0)&&(nota2 >= 0.0 && nota2 <= 10.0))
    {
        printf("valor valido. \n");
        media = (nota1 + nota2)/2;
        printf("A media das notas vale: %.2f \n", media);
    }
    else
    {
        printf("Nao possui um valor valido: \n");
    }

    system("pause");
    return 0;
}
